#+title: Reading PDFs in Emacs
#+date: 2020-06-16
#+draft: true

[[https://github.com/politza/pdf-tools][PDF Tools]] is a way to interact with 
Install some dependancies

Then install the package.

#+begin_src bash
sudo apt-get install libpng-dev zlib1g-dev libpoppler-glib-dev libpoppler-private-dev imagemagick
#+end_src

=M-x package-install RET pdf-tools= and =M-x package-install RET =


Test it out with:

=M-x pdf-info-check-epdfinfo=

Then install:

=M-x pdf-tools-install=

or put in your =.emacs=

#+begin_src emacs-lisp
(pdf-tools-install)

(use-package org-pdftools
  :hook (org-load . org-pdftools-setup-link))
#+end_src


