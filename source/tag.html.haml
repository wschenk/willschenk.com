---
pageable: true
per_page: 12
---
- title "#{tagname.capitalize} articles"
%section.recent-posts.header
  .container
    .row
      .post-list
        %h1
          = "#{tagname.capitalize} articles"

        - if data['topics'][tagname.to_sym]
          %h2= data['topics'][tagname.to_sym][:desc]

%section.recent-posts
  .container
    .row
      .post-list
        = partial "post_list", :locals => {:page_articles => page_articles}
